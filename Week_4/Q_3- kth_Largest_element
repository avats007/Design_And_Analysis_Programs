

#include<iostream> 
#include<bits/stdc++.h> 
using namespace std; 
int main()
{
    int arr[100]; 
    int n;
    int k; 
    cin>>n;
   for(int i=0;i<n;i++)
      {
        cin>>arr[i];
      }
  sort(arr,arr+n); 
  cin>>k; 
  cout<<arr[k-1];
  
  return 0;
}
